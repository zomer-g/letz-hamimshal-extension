{
  "name": "govscraper-extension",
  "version": "0.3.50",
  "private": true,
  "type": "module",
  "description": "Chrome extension that detects and downloads open datasets from Israeli gov.il sites. Tests are zero-dependency Node scripts. The build script is build-zip.cjs (CommonJS); all source + tests are ESM.",
  "scripts": {
    "test": "node tests/run-all.mjs",
    "build": "node build-zip.cjs",
    "build:firefox": "node build-zip.cjs --target=firefox"
  }
}
