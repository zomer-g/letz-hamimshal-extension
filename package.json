{
  "name": "govscraper-extension",
  "version": "0.3.50",
  "license": "MIT",
  "homepage": "https://github.com/zomer-g/govscraper-extension-public",
  "type": "module",
  "description": "Manifest-V3 browser extension that detects and downloads open datasets from Israeli government sites (gov.il, Nadlan, GovMap, Mavat, IDF, geo.mot, Jerusalem licensing) — entirely in the browser. Zero-dependency Node tests; build via build-zip.cjs.",
  "scripts": {
    "test": "node tests/run-all.mjs",
    "build": "node build-zip.cjs",
    "build:firefox": "node build-zip.cjs --target=firefox"
  }
}
